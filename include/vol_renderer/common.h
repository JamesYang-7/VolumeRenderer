#pragma once

#include <string>
#include <GL/glew.h>

const GLuint WIDTH = 800;
const GLuint HEIGHT = 600;
const float CAMERA_RADIUS = 0.7f;
const float CAMERA_SPEED = 90.0f;

// std::string readFile(const std::string& filePath);
